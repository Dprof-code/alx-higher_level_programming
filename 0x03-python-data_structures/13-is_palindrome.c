#include "lists.h"

/**
 * is_palindrome - checks if a singly linked list is a palindrome.
 *
 * @head: pointer to linked list
 * Return: 0 if its not a palindrome, else return 1
 */
int is_palindrome(listint_t **head)
{
	if (head != NULL)
		return (0);
	else
		return (1);
}
